# Document Witness Baseline Snapshot

- Timestamp (UTC): 2026-03-13T16-40-25Z
- Purpose: DL-2 baseline query snapshot pack before canonical witness materialization.

## Canonical witness queries

1) `MATCH (d:DocumentWitness) RETURN count(d)`
- Result: `0`

2) `MATCH (c:Claim)-[:SUPPORTED_BY]->(:DocumentWitness) RETURN count(DISTINCT c)`
- Result: `0`

3) `MATCH (p:Project {projectType:'document'}) RETURN count(p)`
- Result: `0`

## Shadow/IR reference counts (context, not canonical witness proof)

4) `MATCH (n:IRNode) WHERE n.kind IN ['DocumentCollection','DocumentNode','Paragraph'] RETURN n.kind, count(*)`
- DocumentCollection: `3`
- DocumentNode: `101`
- Paragraph: `551`

## Interpretation

Canonical document witness layer is not materialized yet (`DocumentWitness` + document project taxonomy absent).
IR/shadow document artifacts exist and can be used for migration into canonical witness lane.
